parameters: {}
environment: {}
packages:
  - name: upload
    shared: false
    environment: {}
    parameters: {}
    annotations: {}
    functions:
      - name: upload_dumps
        binary: false
        main: "Main"
        runtime: go:1.20
        web: false
        webSecure: false
        parameters: {}
        environment:
          {
            accessKeyID: "${accessKeyID}",
            secretAccessKey: "${secretAccessKey}",
          }
        annotations: {}
        limits: { timeout: 300000 }
        triggers:
          - name: daily
            sourceType: scheduler
            sourceDetails:
              cron: 0 8 * * *
